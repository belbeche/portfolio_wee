{% extends 'base.html.twig' %}

{% block content %}

	<!-- blog start -->
	<section class="blog-area pb-5">
		<div class="container">
			<div class="row justify-content-center">


				{% for Subject in favorites %}

					<div class="col-lg-3 col-md-3 col-sm-10">
						<div class="single-blog blog-style-one">
							<div class="blog-image">
								<a href="{{ path('front_Subjects_show', {'title': Subject.title}) }}">
									{% for image in Subject.images %}
										<img class="card-img-top rounded-0" src="{{ asset('uploads/' ~ image.name) }}" alt="{{ Subject.title }}">
									{% endfor %}
								</a>
								<small>
									{% for category in Subject.categories %}
										<a href="{{ url('front_category', { name: category.name}) }}">{{ category.name }}</a>
									{% endfor %}
								</small>
							</div>
							<div class="blog-content">
								<h5 class="blog-title">
									<a href="{{ path('front_Subjects_show', {'title': Subject.title}) }}">
										<h5 class="card-title">{{ Subject.title }}</h5>
									</a>
								</h5>
								<br>
								<span>
									<i class="bi bi-calendar"></i>
									{{ Subject.createdAt|format_datetime('medium', locale="fr") }}</span>
								<span>
									<i class="bi bi-chat-dots"></i>
									{{ Subject.commentaires|length }}</span>
								<span>
									<i class="bi bi-heart-fill"></i>
									{{ Subject.favoris|length }}</span>
							</div>
						</div>
					</div>
				{% endfor %}
			</div>
		</div>
	</section>

	<!-- ./ End blog start -->

{% endblock %}
